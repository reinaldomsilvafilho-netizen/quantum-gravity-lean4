# Residual fixes, chapters 7–12 (2026-09-25)

Corrector: a Claude subagent that wrote neither the chapters nor the earlier fixes. Inputs were `L2_F37-F40.md`, `L2_F41-F43.md`, finding F-44 of `WORKPLAN.md`, and the companion paper in `Manuscritos_Avulsos/paper_standard_model_masses/` (read only: `CORRECTIONS_2026-09-25.md` items 6–7 and Remark `rem:dft`). Edited files: `chap07`–`chap12`. Chapters 1–6 and 13, `WORKPLAN.md` and `CHANGELOG.md` were not touched.

## Verification scripts

All scripts are in `audit/verify/scripts/`. Each has an independent oracle and a negative control, and every one prints OK.

| Script | Oracle | Negative control |
|---|---|---|
| `residual_ch07_disk.py` | Envelope $\lvert y\rvert\le2-\sqrt{4-(x+1)^2}$ against the disk; forward integration of 2002 curves with $\lvert\kappa\rvert\le1/2$ (random piecewise-constant and both extremal arcs). None avoids the disk | A left–straight–right–straight–left curve with $\lvert\kappa\rvert\le4$ avoids the disk and reaches $q$ |
| `residual_ch10_component.py` | Multistart L-BFGS over all four components of $O(2)^2$, 3 data seeds. Components with $\prod\det=-1$ reach loss 0. $SO(2)^2$ and $(-,-)$ have equal minima: 2.08, 1.74, 1.86 (the referee seed gave 1.37) | A teacher in $SO(2)^2$ is fitted to $10^{-16}$ |
| `residual_ch11_bkm.py` | BKM computed by finite differences of relative entropy, checked against the log-mean formula. 5000 Hilbert–Schmidt qutrits: min 1.001, median 1.155, p95 1.59, max 3.30. Explicit family with $p_1=\varepsilon$: ratio 2.0, 4.3, 8.9 for $\varepsilon=10^{-2},10^{-4},10^{-8}$ (unbounded) | A commuting $X$ gives ratio exactly 1 |
| `residual_ch11_scalar.py` | sympy Riemann tensor of $c_1du^2+c_2e^{2u}dx^2$, $d=2,3,4$: $R=-d(d-1)/c_1$ | Mutation $-d(d-1)/c_2$ fails |
| `residual_ch12_circulant.py` | Numerical `eigh` diagonalization, which does not use the DFT. 200 random circulant pairs give a permutation $\lvert V_{\rm CKM}\rvert$. $\mathbf C^2$ is circulant with eigenvalues $v_j^2$, and the Koide identity $Q=1/3+2b^2/3a^2$ holds | A non-circulant down sector gives row maxima 0.86 (nontrivial mixing) |
| `residual_erfc_prior.py` | Quadrature of $I_0$, $I_1$. SVW eq. (3.7) equals $I_0$ up to their constant normalization (factor 2). The identity $I_1=(\tau^{-1}-I_0)/(2\ell^2)$ holds, and the ch12 closed form matches quadrature to $10^{-7}$ for $\tau\in[10^{-3},10^2]$ | The identity mutated to $/\ell^2$ fails |

The referee scripts `L2_ch10_gap_stiefel.py` (14/14) and the ch7 checks were rerun before editing.

New DOIs, resolved through the Crossref API (title, authors, volume and year all match):
- `10.1016/S1385-7258(58)50051-1`: Świerczkowski, "On a free group of rotations of the euclidean space", Indag. Math. (Proc.) 61 (1958) 376–378.
- `10.2307/2006973`: Culler–Shalen, "Varieties of Group Representations and Splittings of 3-Manifolds", Ann. Math. 117 (1983) 109.
- `10.1103/PhysRevD.84.104018`: Sotiriou–Visser–Weinfurtner, "From dispersion relations to spectral dimension—and back again", PRD 84 (2011). This is arXiv:1105.6098. The full text was read, and eqs. (3.6)–(3.13) contain the error-function and Bessel closed forms.

## Table

| Chapter | Item | Source report | Assessment | Action |
|---|---|---|---|---|
| 7 | Prop. 4.4 / remark after `eq:contact_c11`: "every admissible curve has $\kappa^*>0$" is a non sequitur (an infimum of positive values can be 0) | L2_F37-F40, 7.2 | **Agree.** The conclusion is true. A direct bound holds without any length constraint: for $\lvert\kappa\rvert\le1/2$, $\lvert\sin\varphi\rvert\le(x+1)/2$, so the curve is a graph over $[-1,1]$ with $\lvert y\rvert\le2-\sqrt{4-(x+1)^2}$. That gives $0.146$ at $x=-1/4$, where the disk reaches $0.433$ | Replaced the sentence with the proof of $\kappa^*\ge1/2$, which needs no length bound (so it does not depend on Prop. 5.1) |
| 9 | "(Świerczkowski)" has no bibitem, and "an irreducible pair is determined by traces" has no reference | L2_F37-F40, 9.1 | **Agree** | Added `swierczkowski1958` and `culler1983` (Prop. 1.5.2: irreducible $SL(2,\mathbb C)$ representations with the same character are conjugate). The text now states that the pair is irreducible because it does not commute, that the character of $F_2$ is fixed by $(\operatorname{tr}A,\operatorname{tr}B,\operatorname{tr}AB)$, and that unitary representations conjugate in $GL_2$ are unitarily conjugate |
| 9 | Teardrop remark: "room in the free space" is not enough for a null-homotopic sub-loop | L2_F37-F40, 9.4 | **Agree** (a circle around an obstacle is not null-homotopic) | The hypothesis is now "the free space contains a closed disk of radius $1/\kappa^*$ tangent to that piece", and the boundary circle of that disk is inserted |
| 9 | The abstract implies embedded minimizers for $w\ge2$ | L2_F37-F40, 9.7 | **Agree** | Abstract: "by embedded paths for $w=0,1$ and by immersed, self-intersecting paths for $w\ge2$" |
| 10 | Conj. 3.1 is false as stated (component obstruction) | L2_F37-F40, 10.2 | **Agree.** Reproduced independently on 3 seeds | Hypothesis changed to "$\Sigma^*$ meets the connected component of $O(d)^\ell$ containing $\theta_0$" and $d\ge2$. The remark explains the constancy of $\det W_i$ under descent, the invariance of $\prod\det W_i$ under the tanh symmetries, and the $d=\ell=2$ reflection-teacher counterexample (loss between 1.3 and 2.1 across samples, since 1.37 is seed-specific), plus the degenerate case $d=1$ |
| 10 | "The same happens whenever $D\ge2$, $\mathcal O_{\rm sing}$ bounded…" is unproved | L2_F37-F40, 10.1(a) | **Agree.** The argument uses a constant metric away from the obstacle | Sentence replaced by an explicit non-claim with the reason: cond $\le\Lambda_{\max}$ does not control the Christoffel symbols. "Vanishes" became "can vanish" in the abstract, the remark and the conclusion |
| 10 | "an annulus … which contains the straight segment" | L2_F37-F40, 10.1(b) | **Agree** | Annulus given as $0.012\le\lvert\theta-c\rvert\le0.941$. The segment passes through $c$ and crosses it |
| 11 | "same curvature radius $L$ in the scale and position directions" | L2_F37-F40, 11.1 | **Agree** (sympy: $R=-d(d-1)/c_1$ for every $c_2$) | Rewritten: $c_2$ is removed by $x\mapsto\sqrt{c_2}x$. The invariant content is $c_1=L^2$, and $c_2=L^2/z_0^2$ only fixes the normalization of the boundary coordinate |
| 11 | BKM/SLD "ranges roughly from 1.01 to 1.3" | L2_F37-F40, 11.5 | **Agree** (median 1.155, p95 1.59, unbounded) | States that the ratio is unbounded, with the mechanism (log mean → 0 as $p_i\to0$), and gives median ≈ 1.16 and 95% below 1.6 for HS-random qutrits with random traceless $X$ |
| 11 | Dong–Lewkowycz "obtained integrated versions" | L2_F37-F40, 11.9 | **Agree.** The arXiv 1705.08453 abstract says only "comment on how … can be used to derive" | Now says what the abstract supports: quantum extremality to all orders in $G_N$, and "commented on how these ideas can be used to derive an integrated version of the equations of motion linearized around arbitrary states" |
| 8 | N1: §4.4 title "C^{1,1} Thin Shells" (Israel shells are $C^{0,1}$) | L2_F41-F43, N1 | **Agree** | Title changed to "Israel Junction Conditions and Thin Shells" |
| 8 | N2: Def. 2.3(a) is vacuous in Lorentzian signature | L2_F41-F43, N2 | **Agree** (a spacelike $T_pM$ in a Lorentzian manifold has a normal space containing a timelike vector) | (a) now reads "$g$ is Riemannian", with a parenthetical saying the case does not occur in Lorentzian signature |
| 8 | N3: flat Friedmann assumed; title says "Planck Pressure" | L2_F41-F43, N3 | **Agree.** With $k\le0$, $\Lambda\ge0$: $H^2\ge8\pi G\rho/3$, so the bound holds; with $k=+1$ it does not | Full Friedmann equation written, with the scope stated as $k\le0$, $\Lambda\ge0$ (equality for $k=\Lambda=0$) and a sentence on $k=+1$. Title changed to "…and a Planck Density Bound" |
| 12 | §10.2 does not define $\alpha_t$; the title says "Running of the Tensor Tilt" | L2_F41-F43, F-42 §10.2 | **Agree** | Defined $n_t=d\ln\mathcal P_t/d\ln k$ and $\alpha_t:=dn_t/d\ln k$ (as in ch13). The ansatz is marked as not following from the diffusion model. Title: "An Ansatz for the Running of the Tensor Tilt" |
| 12 | Priority of the erfc closed form (INCERTO) | L2_F41-F43, F-42 | **Resolved: prior art exists.** SVW PRD 84 (2011) 104018, eqs. (3.6)–(3.8), gives the erf closed form for $f=k^2(1+k^2/4K^2)$ (anisotropic, 2+1) and Bessel forms in 3+1. The ch12 isotropic 4D case reduces to theirs by $I_1=(\tau^{-1}-I_0)/(2\ell^2)$. Calcagni (1204.2550, 1205.5046, 1304.2709, 1304.7247, 1311.3340), Modesto (0912.0220) and Calcagni–Modesto–Nardelli (1408.0199) contain no erf/erfc (full-text search) | Cited `sotiriou2011dispersion` with the reduction identity. No novelty claim remains (none was made before) |
| 12 | $K_{ij}$ sign convention opposite to ch8 | L2_F41-F43, obs. | **Agree.** ch12 uses $K=+\tfrac12\mathcal L_n\gamma$ (the sign that goes with its $\pi^{ij}$), and ch8 uses $-\tfrac12\mathcal L_n\gamma$ | Convention stated with ch8's opposite sign flagged explicitly. The Cauchy-slice statements depend only on $\lvert K(v,v)\rvert$, $K_{ij}K^{ij}$ and $K^2$. The thin-shell subsection (sign-sensitive, $K^\theta{}_\theta>0$) uses Israel's $K_{ab}=hh\nabla n$, as in ch8 §4.4. Kept ch12's sign rather than flipping it, because flipping would change $\pi^{ij}$ |
| 12 | F-44: $\mathbf M_f=(v/\sqrt2)\mathbf Y_{\rm circ}$, but the circulant form is applied to root masses; trivial CKM | WORKPLAN F-44 | **Agree** (verified numerically) | Defined $\mathbf C=a\mathbf 1+be^{i\delta}P+be^{-i\delta}P^{-1}$ (eigenvalues $v_j=a+2b\cos(\delta+2\pi j/3)$) and $\mathbf Y_{\rm circ}=(\sqrt2/v)\mathbf C^2$, so $\mathbf M_f=\mathbf C^2$ has eigenvalues $m_j=v_j^2$. The form is stated as an assumption, not a consequence of gauge invariance. The text says that circulant up and down sectors share the DFT diagonalizer, so the CKM matrix is a permutation up to phases, with no Cabibbo angle, and that mixing needs non-circulant structure, which is not supplied. This matches the companion paper's $M=C^2$ and its Remark `rem:dft` |

Disputed items: none. Every item was verified before any change was made.

## Compilation

Each chapter was compiled three times with `pdflatex -interaction=nonstopmode`, both in a clean copy (which avoids stale `.aux` files) and in place. Chapters 7, 8, 9, 10, 11 and 12 all give 0 errors, 0 LaTeX/Package/Class warnings, 0 overfull boxes and 0 undefined references. Page counts are 13, 15, 9, 6, 11 and 17. No theorem environments were added, so the cross-references from ch13 to ch12 (Conj. 4.3, 7.1) and from ch9/ch10 to ch7 keep their numbers. A grep of chapters 7–12 finds no "earlier version", "withdrawn", "corrected", "erratum", "revised", `audit/` or `.py`.

## Items left open

1. **ch10 Conj. 3.1** is still a conjecture, now with the component hypothesis. A version with random initialization and a conclusion in probability, as the referee suggested, was not formulated.
2. **ch10 Remark `rem:length_bound`.** The general statement for bounded $\mathcal O_{\rm sing}$ was withdrawn from the text as a claim, not proved. A correct general version would need, for example, a metric that is flat outside a compact set that avoids $\theta_0$, $\Sigma^*$ and the escaping arcs. Not attempted.
3. **ch12 F-44.** The text now says the circulant ansatz gives no quark mixing. Supplying a mixing mechanism is out of scope.
4. **ch9.** The Culler–Shalen proposition number (1.5.2) is cited from memory of the paper's standard numbering. The DOI and bibliographic data are verified; the internal numbering was not checked against the PDF (JSTOR access).
5. Out of scope, still open from L2_F41-F43: the `adm1962` DOI (reprint versus original) in ch8.

## Rodada 2 (itens de L2_residual)

Input: `L2_residual_2026-09-25.md`, items for ch9 and ch10, and `scripts/L2res_ch10_conj.py`, which I re-ran (0 failures). Only `chap09` and `chap10` were edited.

**Independent check of the ch10 counterexample.** I wrote my own script with an analytic gradient (checked against finite differences to 1.6e-10), located the maximizer by multistart gradient ascent and polished it with Newton's method. It uses the same teacher in $SO(2)^2$ as the referee's control: $W_1=R(0.7)$, $W_2=R(-0.3)$, 40 inputs $\mathcal N(0,2.25I)$, seed 7. Results: the maximum loss on $SO(2)^2$ is 4.1779, $\lvert\nabla\rvert=3\cdot10^{-16}$, and the Hessian eigenvalues are $-4.22$ and $-1.14$, so this is a strict local maximum. One GD step moves the point by exactly 0. GD from a generic start reaches $2\cdot10^{-29}$, so $\Sigma^*$ meets the component. The escape counts 48, 72 and 96 steps come from the referee's script, which I re-ran.

| Chapter | Item | Assessment | Action |
|---|---|---|---|
| 10 | Conj. 3.1 is false for arbitrary $\theta_0$: the loss maximizer on the compact component is a critical point, and there is no uniform step bound near it (M) | **Agree.** Reproduced independently (see above). This is also a general fact: a smooth function on a compact manifold attains its maximum at a critical point, where the Riemannian gradient step is the identity | The conjecture is restated with $\theta_0$ drawn from the normalized Haar measure on a component $K$ that $\Sigma^*$ meets. The conclusion now holds with probability $\ge1-\delta$, with $\lambda_+\ge c(\delta)\ell^{-p}$ and at most $C(\delta)\ell^p$ steps, where $p$ does not depend on $\delta$ and all constants are independent of $\ell$. A new remark, "Why the initialization is random", explains the maximizer obstruction, gives the $d=\ell=2$ example (max about 4.18, a strict local maximum, 48/72/96 steps from distance $10^{-2},10^{-4},10^{-6}$ at step 0.05 in the angles) and the $\log(1/r)$ escape time. It also explains why the constants depend on $\delta$: the Haar measure of a ball scales like $r^{\dim K}$, and the remark assumes that the critical set above $\epsilon$ is closed and of measure zero |
| 9 | Teardrop remark (l. 241): inserting the circle adds length $2\pi/\kappa^*$, so "slack" is not enough (B) | **Agree** | The hypothesis now reads "its length is at least $2\pi/\kappa^*$ below the length bound". The remark also says that the insertion adds length $2\pi/\kappa^*$ and curvature of absolute value $\kappa^*$, so the result is still admissible |
| 9 | Culler–Shalen "Proposition 1.5.2" not checked in the primary text (INCERTO) | **Agree.** I could not check the number in the primary text either | Chose the safe option: the citation is now `\cite{culler1983}` without a proposition number. The bibitem and its DOI 10.2307/2006973 are unchanged and were already verified by the referee on Crossref. No new reference or DOI was added, so there was no new DOI to check |

**Compilation.** Both chapters were compiled with `pdflatex -interaction=nonstopmode`, three times each, both in a clean output directory and in place. ch9 has 9 pages and ch10 has 6. Each has 0 errors, 0 LaTeX/Package/Class warnings, 0 overfull boxes and 0 undefined references. The new remark in ch10 comes after Conj. 3.1 and shifts only the number of the following remark in §3. No other chapter references ch10 numbers directly. A grep of both chapters finds no revision-history wording and no `audit/` paths.
